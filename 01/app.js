const user = {
    firstName : "Adam",
    lastName : "Kowalski",
    sex : "Mężczyzna",
    age : 43,
};

console.log("Imię: " + user.firstName + " Nazwisko: " + user.lastName + " płeć: " + user.sex + " wiek: " + user.age);