const user = {
  firstName: "Ola",
  lastName: "Kowalska",
  sex: "female",
  age: 25,
};

console.log(user);
console.log(
  `firstName:${user.firstName}, lastName:${user.lastName}, sex:${user.sex}, age:${user.age}`
);
