const user = {
    firstName: 'Kuba',
    lastName: 'Jakub',
    sex: 'm',
    age: 30,
}

// console.log(user)

console.log(user.firstName,
            user.lastName,
            user.sex,
            user.age)
